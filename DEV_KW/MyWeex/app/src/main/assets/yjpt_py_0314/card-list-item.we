<template>
    <div class="row" repeat="{{itemList}}">
        <div class="photo">
            <image class="headPic" if="{{photo && state}}"  style=" border-radius: {{state}} " src="{{photo}}"></image>
            <image class="headPic" if="{{photo && !state}}" src="{{photo}}"></image>
        </div>
        <div class="cake_right">
            <div class="cake-list" >
                <text class="cake-list-name">{{name}}</text>
                <text class="cake-list-time" >{{date}}</text>
            </div>
            <text class="cakeContent">{{content}}</text>
            <div class="pic_list">
                  <image class="pic_li" repeat="img" src="{{picUrl}}"></image>
            </div>
             <review-item review-list="{{reviewList}}"></review-item>
             
        </div> 
    </div>   
</template>

<style>
  .row{ border-bottom-width: 1; border-bottom-color: #e7e7e7; border-left-style: solid; padding:15; flex-direction: row;} 
  .photo{ padding: 10; padding-right: 25;}
  .headPic {width: 110; height: 110;}
  .cake_right{flex: 3;}
  .cake-list{flex-direction: row; justify-content: space-between;}
  .cake-list-name{font-size:38;  height: 80; line-height: 60;}
  .cake-list-time{color: #f80; height: 80; line-height: 60; }
  .cakeContent{ font-size:36; line-height:45; margin-bottom: 10; }

  .pic_list{flex-direction: row; flex-wrap:wrap; }
  .pic_li{height:140; width: 170; margin: 10; }
</style>
<script>
  require('weex-components');
  var Utils = require('./javascript/Utils');
  module.exports = {
    data: {
     itemList: [],
     reviewList: []
    },
    created : function(){
            

             // Utils.changeImg(self.images,['blankPic','selectPic']);
              /*for(var i in self.reviewList){
              self.itemList[i].imgUrl = self.images.blankPic;
              self.itemList[0].imgUrl = self.images.selectPic;
              }*/
        },
    methods: {
     /* tabShow:function(index){
          if(this.reviewList[index].pictureUrl == this.images.blankRadio){
              for(var i in this.reviewList){
                  this.reviewList[i].pictureUrl = this.images.blankRadio;
              }
              this.reviewList[index].pictureUrl = this.images.selectRadio;
          }
      }*/
    /*isShow: function(){
         this.isShow = this.isShow == false ? true : false;
      }*/
    }
  }
</script>

