<template> 
      <div class="item" onclick="openPage(url)">
          <div class="list" style=" flex:3;">
              <image class="pic_thumb1" if="{{titlePicUrl && !stateOne}}" src="{{titlePicUrl}}"></image>
              <text if="{{titleName}}" class="titleName">{{titleName}}</text>
          </div>
          <div class="list">
            <image class="pic_thumb"  style=" border-radius: {{state}} " if="{{pic_thumb && state}}" src="{{pic_thumb}}"></image>
            <img style=" width: 22; height: 35;" if="{{src}}" src="{{src}}">
          </div>
      </div>
</template>

<style>
  .item {
    width: 750;  background-color:#fff;
    border-bottom-width: 1;
    border-bottom-color: #e7e7e7;
    border-bottom-style: solid;
    padding:28;
    flex-direction: row;
    display: flex; align-items: center; justify-content:space-between;
  }
  .list{flex-direction: row; display: flex; align-items:center;justify-content:space-around;}
  .titleName{font-size: 38; }
  .content{margin-left: 20; color: #777; font-size: 34; }
  .pic_thumb { width: 95; height: 95; margin-right:15; margin-left: 8;}
  .pic_thumb1 { width: 60; height: 60; margin-right:15;}
</style>

<script>
  require('weex-components');
  var Utils = require('./javascript/Utils');
  var modal = require('@weex-module/modal');
  module.exports = {
    data: {
      dir: 'templates',
      src:'',
      pic_thumb:'',
      pic_thumb1:''
    },
    created:function(){},
    methods : {
      openPage : function(url){
        console.log(url);
        // Utils.navigate.push(this,url,'true');
      }
    } 
  }
</script>
